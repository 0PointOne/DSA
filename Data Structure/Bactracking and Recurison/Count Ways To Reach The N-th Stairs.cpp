#include <bits/stdc++.h>
using namespace std;



int main(){
    int t;
    cin >> t;
    while(t--){
        int ArrSize, targetSum;
        cin >> ArrSize >> targetSum;
        int arr[ArrSize];
        for(int i = 0; i < ArrSize; i++){
            cin >> arr[i];
        }
        int key;    cin >> key;
        int subseque(arr, key);
    }
}