#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;  cin >> n;
    vector<int> v(n);
    
    int sum;    cin >> sum;

    vector<vector<bool> > dp(1e3, vector<bool> (1e3, false));

    for(int i = 1; i <= n; i++){
        for(int j = 0; j <= sum; j++){

        }
    }
    return 0;
}